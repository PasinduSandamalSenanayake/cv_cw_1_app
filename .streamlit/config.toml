[global]

# If false, makes your Streamlit script default to "headless" mode
# which prevents some warnings.
headless = true

# Automatically rerun script when the file is modified on disk.
autoRerun = false

# List of folders that should not be watched for changes.
# This impacts both "Run on Save" and "Always rerun".
watchFolders = ['.']

[server]

# List of folders that should not be watched for changes.
folderWatchBlacklist = ['.']

# Change the type of file watcher used by Streamlit, or turn it off completely.
fileWatcherType = "none"

# Symmetric key used to produce signed cookies. If deploying on multiple replicas,
# this should be set to the same value across all replicas to ensure they all
# share the same secret.
# cookieSecret = "some-secret"

# If false, will attempt to open a new tab or window every time Streamlit is
# called. If true, will only open a new tab the first time Streamlit is called.
reuseConnection = true

# Controls whether uncaught app exceptions are displayed in the browser. By
# default, this is set to True and Streamlit displays app exceptions and
# associated tracebacks in the browser.
showErrorDetails = false
